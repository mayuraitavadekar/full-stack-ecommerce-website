import React from "react";
import Base from "../core/Base";

const userDashBoard = () => {
  return (
    <Base title="User Dashboard" description="this is user dashboard page!">
      <h1>This is user dashboard page</h1>
    </Base>
  );
};

export default userDashBoard;
